# Models

Do not commit model weight files (`*.pt`) to Git.

`ultralytics` downloads required weights automatically at first run, for example:

- `yolov8n.pt`
- `yolov8n-pose.pt`
- `yolov8m.pt`
- `yolov8m-pose.pt`
